.login
  .btn= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook)
  .btn{ 'title' => 'Login here' }
    = link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2)

%h1 Welcome to Moving Teachings
%h3.muted Learn new things on the bus (or metro, or subway, or streetcar)

Take mini-classes from random people.

It's completely anonymous and fun.

.btn{ 'ng-click' => 'loadSampleClasses()' } Browse sample classes

.classes{ 'ng-show' => 'samples' }
  .wrapper{ 'ng-repeat' => 'class in samples' }
    = render partial: 'classes/dialectic'


